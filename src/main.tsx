import { StrictMode, useMemo, useState } from 'react'
import { createRoot } from 'react-dom/client'
import { Activity, AlertTriangle, ArrowUpRight, Bell, Check, ChevronRight, ClipboardCheck, FileCheck2, FileText, Filter, Gauge, LayoutDashboard, Menu, MoreHorizontal, Search, ShieldCheck, SlidersHorizontal, UserRound, Users, X } from 'lucide-react'
import './index.css'

type Bidder = { name: string; category: string; score: number; status: 'Compliant' | 'Review' | 'At risk'; docs: string; risk: string }
const bidders: Bidder[] = [
  { name: 'Apex Industrial Systems', category: 'OEM / Manufacturer', score: 96, status: 'Compliant', docs: '18 / 18', risk: 'Low' },
  { name: 'Bharat Process Equipments', category: 'Authorized reseller', score: 82, status: 'Review', docs: '16 / 18', risk: 'Medium' },
  { name: 'Coromandel Engineering Co.', category: 'MSME / Manufacturer', score: 71, status: 'At risk', docs: '14 / 18', risk: 'High' },
]
const requirements = [
  ['GST registration', 'GSTIN 33AAACA1234A1Z5', 'Apex GST Certificate.pdf', 'Page 1', 'Verified'],
  ['Udyam / MSME registration', 'UDYAM-TN-02-0012345', 'Udyam Registration.pdf', 'Page 1', 'Verified'],
  ['OEM authorization', 'Valid until 31 Dec 2026', 'OEM Authorization Letter.pdf', 'Page 2', 'Review'],
  ['Experience threshold', '3 comparable contracts found', 'Work Order Bundle.pdf', 'Pages 4–11', 'Verified'],
]
function App() {
  const [active, setActive] = useState('Overview')
  const [selectedBidder, setSelectedBidder] = useState(bidders[0])
  const [verificationRunning, setVerificationRunning] = useState(false)
  const [decision, setDecision] = useState<string | null>(null)
  const [search, setSearch] = useState('')
  const filtered = useMemo(() => bidders.filter(b => b.name.toLowerCase().includes(search.toLowerCase())), [search])
  const nav = [{ label: 'Overview', icon: LayoutDashboard }, { label: 'Tenders', icon: FileText }, { label: 'Bidder review', icon: Users }, { label: 'Verification', icon: ShieldCheck }, { label: 'Reports', icon: ClipboardCheck }, { label: 'Audit trail', icon: Activity }]
  return <div className="min-h-screen bg-background text-foreground"><aside className="fixed inset-y-0 left-0 hidden w-64 border-r border-border bg-panel lg:flex lg:flex-col"><div className="flex h-20 items-center gap-3 border-b border-border px-6"><div className="grid size-9 place-items-center rounded-lg bg-brand text-background font-bold">CP</div><div><p className="text-sm font-semibold tracking-wide">CPCL</p><p className="text-xs text-muted">Procurement OS</p></div></div><div className="p-4"><p className="px-3 pb-3 text-[10px] font-bold uppercase tracking-[0.2em] text-muted">Workspace</p><nav className="flex flex-col gap-1">{nav.map(({ label, icon: Icon }) => <button key={label} onClick={() => setActive(label)} className={`flex items-center gap-3 rounded-md px-3 py-2.5 text-sm transition ${active === label ? 'bg-panel-soft text-brand' : 'text-muted hover:bg-panel-soft hover:text-foreground'}`}><Icon size={17}/>{label}{label === 'Verification' && <span className="ml-auto rounded-full bg-warning/15 px-2 py-0.5 text-[10px] text-warning">3</span>}</button>)}</nav></div><div className="mt-auto border-t border-border p-4"><div className="flex items-center gap-3 rounded-md bg-panel-soft p-3"><div className="grid size-8 place-items-center rounded-full bg-brand/15 text-xs font-semibold text-brand">AR</div><div className="min-w-0"><p className="truncate text-sm">Ananya Rao</p><p className="truncate text-xs text-muted">Procurement officer</p></div><MoreHorizontal className="ml-auto text-muted" size={16}/></div></div></aside><main className="lg:pl-64"><header className="flex h-20 items-center justify-between border-b border-border px-5 sm:px-8"><div className="flex items-center gap-3"><button className="rounded-md p-2 text-muted lg:hidden"><Menu size={20}/></button><div><p className="text-xs text-muted">Ministry of Petroleum & Natural Gas / CPCL</p><h1 className="mt-1 text-xl font-semibold tracking-tight">{active}</h1></div></div><div className="flex items-center gap-2"><button className="rounded-md p-2 text-muted hover:bg-panel-soft"><Bell size={18}/></button><div className="mx-1 hidden h-6 w-px bg-border sm:block"/><div className="grid size-8 place-items-center rounded-full bg-panel-soft text-xs font-semibold">AR</div></div></header><div className="mx-auto max-w-[1400px] p-5 sm:p-8"><div className="mb-8 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><div className="mb-3 flex items-center gap-2"><span className="rounded-full border border-brand/30 bg-brand/10 px-2 py-1 text-[10px] font-bold uppercase tracking-[0.16em] text-brand">Live tender workspace</span><span className="text-xs text-muted">Last synced 2 min ago</span></div><h2 className="max-w-2xl text-3xl font-semibold tracking-tight sm:text-4xl">Good morning, Ananya.</h2><p className="mt-2 max-w-xl text-sm leading-6 text-muted">Review the latest bid compliance signals before the evaluation committee meets.</p></div><button onClick={() => { setVerificationRunning(true); setTimeout(() => setVerificationRunning(false), 1800) }} className="flex items-center justify-center gap-2 rounded-md bg-brand px-4 py-2.5 text-sm font-semibold text-background transition hover:brightness-110"><ShieldCheck size={17}/>{verificationRunning ? 'Verification running…' : 'Run verification'}</button></div><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric icon={FileText} label="Active tenders" value="08" detail="2 closing this week"/><Metric icon={Users} label="Bidders under review" value="24" detail="6 need attention"/><Metric icon={Gauge} label="Avg. compliance score" value="84.6%" detail="↑ 8.2% this month"/><Metric icon={AlertTriangle} label="Open risk signals" value="07" detail="3 high priority" tone="warning"/></section><div className="mt-6 grid gap-6 xl:grid-cols-[1.35fr_0.65fr]"><section className="rounded-lg border border-border bg-panel"><div className="flex flex-col gap-4 border-b border-border p-5 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-muted">Current tender</p><h3 className="mt-2 text-lg font-semibold">CPCL / Mechanical Seals & Spares</h3><p className="mt-1 text-xs text-muted">Tender ID GEM/2026/B/4819201 · Evaluation in progress</p></div><span className="flex items-center gap-1.5 text-xs text-warning"><span className="size-2 rounded-full bg-warning"/>Human review required</span></div><div className="grid gap-4 p-5 sm:grid-cols-3"><MiniStat label="Closing date" value="24 Sep 2026"/><MiniStat label="Total bids" value="12 bidders"/><MiniStat label="Documents" value="216 files"/></div><div className="border-t border-border p-5"><div className="mb-4 flex items-center justify-between"><div><h4 className="font-semibold">Bidder comparison</h4><p className="mt-1 text-xs text-muted">Explainable scores from submitted evidence</p></div><button className="flex items-center gap-1 text-xs text-brand">View all <ArrowUpRight size={14}/></button></div><div className="mb-4 flex flex-col gap-3 sm:flex-row"><label className="relative flex-1"><Search className="absolute left-3 top-2.5 text-muted" size={16}/><input value={search} onChange={e => setSearch(e.target.value)} placeholder="Search bidders" className="w-full rounded-md border border-border bg-background py-2 pl-9 pr-3 text-sm outline-none focus:border-brand"/></label><button className="flex items-center justify-center gap-2 rounded-md border border-border px-3 text-sm text-muted hover:text-foreground"><Filter size={15}/> Filters</button></div><div className="overflow-x-auto"><table className="w-full min-w-[620px] text-left text-sm"><thead className="border-b border-border text-xs text-muted"><tr><th className="pb-3 font-medium">Bidder</th><th className="pb-3 font-medium">Score</th><th className="pb-3 font-medium">Documents</th><th className="pb-3 font-medium">Risk</th><th className="pb-3"/></tr></thead><tbody>{filtered.map(b => <tr key={b.name} onClick={() => setSelectedBidder(b)} className={`cursor-pointer border-b border-border/70 transition last:border-0 hover:bg-panel-soft ${selectedBidder.name === b.name ? 'bg-panel-soft' : ''}`}><td className="py-4"><p className="font-medium">{b.name}</p><p className="mt-1 text-xs text-muted">{b.category}</p></td><td className="py-4"><div className="flex items-center gap-2"><span className={`text-base font-semibold ${b.score > 90 ? 'text-brand' : b.score > 75 ? 'text-warning' : 'text-danger'}`}>{b.score}%</span><span className="text-[10px] text-muted">{b.status}</span></div></td><td className="py-4 text-muted">{b.docs}</td><td className="py-4"><StatusPill status={b.risk}/></td><td className="py-4 text-right text-muted"><ChevronRight size={16}/></td></tr>)}</tbody></table></div></div></section><aside className="flex flex-col gap-6"><section className="rounded-lg border border-border bg-panel p-5"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-muted">Selected bidder</p><h3 className="mt-2 font-semibold">{selectedBidder.name}</h3><p className="mt-1 text-xs text-muted">{selectedBidder.category}</p></div><button className="text-muted"><MoreHorizontal size={17}/></button></div><div className="my-5 flex items-center gap-5"><div className="relative grid size-24 place-items-center rounded-full" style={{background:`conic-gradient(#5ed6d1 ${selectedBidder.score}%, #263746 0)`}}><div className="grid size-[76px] place-items-center rounded-full bg-panel"><span className="text-2xl font-semibold">{selectedBidder.score}</span><span className="text-[10px] text-muted">/ 100</span></div></div><div><p className="text-sm font-medium">Compliance score</p><p className="mt-1 text-xs leading-5 text-muted">Based on deterministic rules, verified evidence, and officer review.</p></div></div><div className="grid grid-cols-2 gap-3"><MiniStat label="Documents" value={selectedBidder.docs}/><MiniStat label="Risk level" value={selectedBidder.risk}/></div><button className="mt-5 flex w-full items-center justify-center gap-2 rounded-md border border-border py-2.5 text-sm font-medium hover:bg-panel-soft">Open bidder workspace <ArrowUpRight size={15}/></button></section><section className="rounded-lg border border-border bg-panel p-5"><div className="flex items-center justify-between"><div><h3 className="font-semibold">Verification pipeline</h3><p className="mt-1 text-xs text-muted">Demo source · deterministic checks</p></div><Activity size={18} className="text-brand"/></div><div className="mt-5 flex flex-col gap-4">{[['Document extraction','216 / 216 complete',true],['Field validation','214 / 216 complete',true],['Requirement matching','In progress',false],['External verification','Queued',false]].map(([label, detail, done]) => <div key={String(label)} className="flex gap-3"><div className={`mt-0.5 grid size-5 shrink-0 place-items-center rounded-full ${done ? 'bg-brand text-background' : 'border border-border text-muted'}`}>{done ? <Check size={12}/> : <span className="size-1.5 rounded-full bg-muted"/>}</div><div><p className="text-sm">{label}</p><p className="mt-0.5 text-xs text-muted">{detail}</p></div></div>)}</div></section></aside></div><section className="mt-6 rounded-lg border border-border bg-panel"><div className="flex flex-col gap-3 border-b border-border p-5 sm:flex-row sm:items-center sm:justify-between"><div><h3 className="font-semibold">Evidence requiring attention</h3><p className="mt-1 text-xs text-muted">Review source documents before recording a final decision.</p></div><button className="flex items-center gap-1 text-xs text-brand">Open evidence queue <ArrowUpRight size={14}/></button></div><div className="overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead className="text-xs text-muted"><tr><th className="px-5 pb-2 pt-4 font-medium">Requirement</th><th className="pb-2 pt-4 font-medium">Extracted value</th><th className="pb-2 pt-4 font-medium">Source document</th><th className="pb-2 pt-4 font-medium">Evidence</th><th className="pb-2 pt-4 font-medium">State</th></tr></thead><tbody>{requirements.map(([req, value, doc, page, state]) => <tr key={req} className="border-t border-border/70"><td className="px-5 py-4 font-medium">{req}</td><td className="py-4 text-muted">{value}</td><td className="py-4"><div className="flex items-center gap-2"><FileCheck2 size={15} className="text-brand"/><span>{doc}</span></div></td><td className="py-4 text-xs text-muted">{page}</td><td className="py-4"><StatusPill status={state}/></td></tr>)}</tbody></table></div></section><section className="mt-6 flex flex-col gap-4 rounded-lg border border-warning/30 bg-warning/5 p-5 sm:flex-row sm:items-center sm:justify-between"><div className="flex gap-3"><AlertTriangle className="mt-0.5 shrink-0 text-warning" size={20}/><div><p className="font-semibold">Officer decision required</p><p className="mt-1 text-sm leading-6 text-muted">AI assistance is advisory only. Confirm the evidence and record your final tender decision for <span className="text-foreground">{selectedBidder.name}</span>.</p></div></div><div className="flex gap-2"><button onClick={() => setDecision('Needs clarification')} className="rounded-md border border-border px-3 py-2 text-sm hover:bg-panel-soft">Request clarification</button><button onClick={() => setDecision('Reviewed')} className="rounded-md bg-brand px-3 py-2 text-sm font-semibold text-background hover:brightness-110">{decision ?? 'Mark as reviewed'}</button></div></section></div></main></div>
}
function Metric({ icon: Icon, label, value, detail, tone }: { icon: typeof Activity; label: string; value: string; detail: string; tone?: string }) { return <div className="rounded-lg border border-border bg-panel p-5"><div className="flex items-center justify-between"><span className="text-xs text-muted">{label}</span><Icon size={17} className={tone === 'warning' ? 'text-warning' : 'text-brand'}/></div><p className="mt-4 text-3xl font-semibold tracking-tight">{value}</p><p className={`mt-2 text-xs ${tone === 'warning' ? 'text-warning' : 'text-muted'}`}>{detail}</p></div> }
function MiniStat({ label, value }: { label: string; value: string }) { return <div className="rounded-md bg-panel-soft p-3"><p className="text-[10px] uppercase tracking-[0.12em] text-muted">{label}</p><p className="mt-1.5 text-sm font-medium">{value}</p></div> }
function StatusPill({ status }: { status: string }) { const tone = status === 'Verified' || status === 'Low' || status === 'Compliant' ? 'text-brand bg-brand/10' : status === 'High' || status === 'At risk' ? 'text-danger bg-danger/10' : 'text-warning bg-warning/10'; return <span className={`rounded-full px-2 py-1 text-[10px] font-medium ${tone}`}>{status}</span> }

createRoot(document.getElementById('root')!).render(<StrictMode><App /></StrictMode>)
